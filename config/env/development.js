module.exports = {
    port: 3001,
    db: "mongodb://localhost/kanban",
    secretKey: `Amazingly secret, so secret no one will ever guess! Hopefully.
        Anyway, this is just for the dev environment anyway, so who cares?`,
};
